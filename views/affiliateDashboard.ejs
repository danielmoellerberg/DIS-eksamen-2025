<!DOCTYPE html>
<html lang="da">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Understory</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        .dashboard-container {
            min-height: 100vh;
            background: #f4f1e8;
        }

        .dashboard-header {
            background: linear-gradient(135deg, #1a5029 0%, #06290f 100%);
            padding: 24px 48px;
            color: white;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .dashboard-nav {
            display: flex;
            gap: 24px;
            align-items: center;
        }

        .dashboard-nav a {
            color: white;
            text-decoration: none;
            padding: 8px 16px;
            border-radius: 8px;
            transition: background 0.3s ease;
        }

        .dashboard-nav a:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .logout-btn {
            background: rgba(255, 255, 255, 0.2);
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
            color: white;
            transition: all 0.3s ease;
        }

        .logout-btn:hover {
            background: rgba(255, 255, 255, 0.3);
        }

        .dashboard-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 48px 24px;
        }

        .welcome-card {
            background: white;
            border-radius: 16px;
            padding: 40px;
            margin-bottom: 32px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
        }

        .welcome-card h2 {
            color: #06290f;
            margin-top: 0;
            margin-bottom: 12px;
        }

        .welcome-card p {
            color: #666;
            font-size: 16px;
            line-height: 1.6;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 24px;
            margin-top: 32px;
        }

        .info-card {
            background: white;
            border-radius: 12px;
            padding: 24px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
        }

        .info-card h3 {
            color: #1a5029;
            margin-top: 0;
            margin-bottom: 8px;
            font-size: 16px;
        }

        .info-card p {
            color: #666;
            margin: 0;
            font-size: 14px;
        }

        .cta-section {
            background: linear-gradient(135deg, #1a5029 0%, #06290f 100%);
            color: white;
            border-radius: 16px;
            padding: 40px;
            text-align: center;
            margin-top: 32px;
        }

        .cta-section h3 {
            margin-top: 0;
            margin-bottom: 16px;
            font-size: 24px;
        }

        .cta-section p {
            margin-bottom: 24px;
            opacity: 0.9;
        }

        .cta-btn {
            background: #f1ff67;
            color: #06290f;
            padding: 14px 32px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            display: inline-block;
            transition: all 0.3s ease;
        }

        .cta-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(241, 255, 103, 0.4);
        }

        .experiences-section {
            margin-top: 60px;
        }

        .experiences-section h2 {
            color: #06290f;
            margin-bottom: 32px;
        }

        .experiences-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 24px;
        }

        .experience-card {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
        }

        .experience-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        }

        .experience-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
            background: #e0e0e0;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #999;
            font-size: 14px;
        }

        .experience-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .experience-content {
            padding: 20px;
        }

        .experience-title {
            color: #06290f;
            font-size: 20px;
            font-weight: 600;
            margin: 0 0 8px 0;
        }

        .experience-category {
            display: inline-block;
            background: #1a5029;
            color: white;
            padding: 4px 12px;
            border-radius: 12px;
            font-size: 12px;
            margin-bottom: 12px;
        }

        .experience-description {
            color: #666;
            font-size: 14px;
            line-height: 1.6;
            margin-bottom: 16px;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .experience-meta {
            display: flex;
            gap: 16px;
            margin-bottom: 16px;
            flex-wrap: wrap;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 6px;
            color: #666;
            font-size: 14px;
        }

        .experience-actions {
            display: flex;
            gap: 12px;
            padding-top: 16px;
            border-top: 1px solid #e0e0e0;
        }

        .btn-edit {
            background: #1976d2;
            color: white;
            padding: 8px 16px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            font-family: 'Inter', sans-serif;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }

        .btn-edit:hover {
            background: #1565c0;
        }

        .btn-delete {
            background: #d32f2f;
            color: white;
            padding: 8px 16px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 14px;
            font-family: 'Inter', sans-serif;
            transition: all 0.3s ease;
        }

        .btn-delete:hover {
            background: #b71c1c;
        }

        .bookings-section {
            margin-top: 60px;
        }

        .bookings-section h2 {
            color: #06290f;
            margin-bottom: 24px;
        }

        .bookings-table {
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
        }

        .bookings-table table {
            width: 100%;
            border-collapse: collapse;
        }

        .bookings-table th {
            background: #1a5029;
            color: white;
            padding: 16px;
            text-align: left;
            font-weight: 600;
        }

        .bookings-table td {
            padding: 16px;
            border-bottom: 1px solid #e0e0e0;
        }

        .bookings-table tr:last-child td {
            border-bottom: none;
        }

        .bookings-table tr:hover {
            background: #f5f5f5;
        }

        .status-badge {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 600;
        }

        .status-confirmed {
            background: #e8f5e9;
            color: #2e7d32;
        }

        .status-pending {
            background: #fff3e0;
            color: #f57c00;
        }

        .status-cancelled {
            background: #ffebee;
            color: #c62828;
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <header class="dashboard-header">
            <nav class="dashboard-nav">
                <a href="/affiliate/logout" class="logout-btn">Log ud</a>
            </nav>
        </header>

        <main class="dashboard-content">
            <% if (typeof success !== 'undefined' && success) { %>
                <div class="success-message" style="background: #e8f5e9; color: #2e7d32; padding: 12px 16px; border-radius: 10px; font-size: 14px; border-left: 4px solid #4caf50; margin-bottom: 24px;">
                    <%= success %>
                </div>
            <% } %>

            <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-message" style="background: #ffebee; color: #c62828; padding: 12px 16px; border-radius: 10px; font-size: 14px; border-left: 4px solid #c62828; margin-bottom: 24px;">
                    <%= error %>
                </div>
            <% } %>

            <div class="welcome-card">
                <h2>Velkommen, <%= partner.name %>! 👋</h2>
                <p>
                    Du er nu logget ind som affiliate partner på Understory. 
                    Herfra kan du administrere dine oplevelser og se statistikker.
                </p>
            </div>

            <div class="info-grid">
                <div class="info-card">
                    <h3>🎯 Aktive Oplevelser</h3>
                    <p style="font-size: 32px; font-weight: 700; color: #1a5029; margin-top: 8px;">
                        <%= statistics.activeExperiences %>
                    </p>
                </div>

                <div class="info-card">
                    <h3>📅 Samlet Antal Bookinger</h3>
                    <p style="font-size: 32px; font-weight: 700; color: #1a5029; margin-top: 8px;">
                        <%= statistics.totalBookings %>
                    </p>
                </div>

                <div class="info-card">
                    <h3>💰 Samlet Omsætning</h3>
                    <p style="font-size: 32px; font-weight: 700; color: #1a5029; margin-top: 8px;">
                        <%= statistics.totalRevenue.toLocaleString('da-DK') %> kr.
                    </p>
                </div>
            </div>

            <div class="cta-section">
                <h3>Klar til at oprette din første oplevelse?</h3>
                <p>Del dine unikke oplevelser med kunder på Understory Marketplace</p>
                <a href="/affiliate/experiences/create" class="cta-btn">Opret Oplevelse</a>
            </div>

            <!-- MINE OPLEVELSER -->
            <% if (experiences && experiences.length > 0) { %>
                <section class="experiences-section">
                    <h2>Mine Oplevelser</h2>
                    <div class="experiences-grid">
                        <% experiences.forEach(exp => { %>
                            <div class="experience-card">
                                <div class="experience-image">
                                    <% if (exp.image_url) { %>
                                        <img src="<%= exp.image_url %>" alt="<%= exp.title %>">
                                    <% } else { %>
                                        Intet billede
                                    <% } %>
                                </div>
                                <div class="experience-content">
                                    <span class="experience-category"><%= exp.category || 'Andet' %></span>
                                    <h3 class="experience-title"><%= exp.title %></h3>
                                    <p class="experience-description"><%= exp.description %></p>
                                    
                                    <div class="experience-meta">
                                        <div class="meta-item">
                                            📍 <%= exp.location %>
                                        </div>
                                        <div class="meta-item">
                                            ⏱️ <%= exp.duration_hours %> timer
                                        </div>
                                        <div class="meta-item">
                                            💰 <%= exp.price_from %> kr.
                                        </div>
                                    </div>

                                    <div class="experience-actions">
                                        <a href="/affiliate/experiences/edit/<%= exp.id %>" class="btn-edit">✏️ Rediger</a>
                                        <form action="/affiliate/experiences/delete/<%= exp.id %>" method="POST" 
                                              onsubmit="return confirm('Er du sikker på at du vil slette denne oplevelse?');" 
                                              style="display: inline;">
                                            <button type="submit" class="btn-delete">🗑️ Slet</button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </section>
            <% } %>

            <!-- BOOKINGS OVERSIGT -->
            <section class="bookings-section">
                <h2>📅 Bookinger</h2>
                
                <% if (bookings && bookings.length > 0) { %>
                    <div class="bookings-table">
                        <table>
                            <thead>
                                <tr>
                                    <th>Oplevelse</th>
                                    <th>Kunde</th>
                                    <th>Email</th>
                                    <th>Telefon</th>
                                    <th>Dato</th>
                                    <th>Deltagere</th>
                                    <th>Pris</th>
                                    <th>Status</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% bookings.forEach(booking => { %>
                                    <tr>
                                        <td><strong><%= booking.experience_title %></strong></td>
                                        <td><%= booking.customer_name %></td>
                                        <td><%= booking.customer_email %></td>
                                        <td><%= booking.customer_phone || '-' %></td>
                                        <td><%= new Date(booking.booking_date).toLocaleDateString('da-DK') %></td>
                                        <td><%= booking.number_of_participants %></td>
                                        <td><%= booking.total_price %> kr.</td>
                                        <td>
                                            <% if (booking.status === 'confirmed') { %>
                                                <span class="status-badge status-confirmed">Bekræftet</span>
                                            <% } else if (booking.status === 'pending') { %>
                                                <span class="status-badge status-pending">Afventer</span>
                                            <% } else if (booking.status === 'cancelled') { %>
                                                <span class="status-badge status-cancelled">Annulleret</span>
                                            <% } else { %>
                                                <span class="status-badge"><%= booking.status %></span>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                <% } else { %>
                    <div class="empty-state">
                        <h3>Ingen bookinger endnu</h3>
                        <p>Når folk booker dine oplevelser, vil de vises her.</p>
                    </div>
                <% } %>
            </section>
        </main>
    </div>
</body>
</html>


<!DOCTYPE html>
<html lang="da">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        .auth-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            background: linear-gradient(135deg, #1a5029 0%, #06290f 100%);
        }

        .auth-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 48px;
            max-width: 450px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        }

        .auth-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .auth-header h1 {
            color: #06290f;
            font-size: 32px;
            margin-bottom: 8px;
        }

        .auth-header p {
            color: #1a5029;
            font-size: 16px;
        }

        .auth-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-group label {
            color: #06290f;
            font-weight: 500;
            font-size: 14px;
        }

        .form-group input {
            padding: 14px 16px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 16px;
            font-family: 'Inter', sans-serif;
            transition: all 0.3s ease;
        }

        .form-group input:focus {
            outline: none;
            border-color: #1a5029;
            box-shadow: 0 0 0 3px rgba(26, 80, 41, 0.1);
        }

        .error-message {
            background: #ffebee;
            color: #c62828;
            padding: 12px 16px;
            border-radius: 10px;
            font-size: 14px;
            border-left: 4px solid #c62828;
        }

        .submit-btn {
            background: #1a5029;
            color: white;
            padding: 16px;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Inter', sans-serif;
        }

        .submit-btn:hover {
            background: #06290f;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(26, 80, 41, 0.4);
        }

        .auth-footer {
            text-align: center;
            margin-top: 24px;
            color: #666;
            font-size: 14px;
        }

        .auth-footer a {
            color: #1a5029;
            text-decoration: none;
            font-weight: 600;
        }

        .auth-footer a:hover {
            text-decoration: underline;
        }

        .back-link {
            display: inline-block;
            color: #1a5029;
            text-decoration: none;
            margin-bottom: 20px;
            font-size: 14px;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        .form-hint {
            color: #666;
            font-size: 12px;
            margin-top: -4px;
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <div class="auth-card">
            <a href="/" class="back-link">← Tilbage til forsiden</a>
            
            <div class="auth-header">
                <h1>Opret Partner Konto</h1>
                <p>Begynd at dele dine oplevelser på Understory</p>
            </div>

            <% if (error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
            <% } %>

            <form action="/affiliate/register" method="POST" class="auth-form">
                <div class="form-group">
                    <label for="name">Virksomhedsnavn</label>
                    <input 
                        type="text" 
                        id="name" 
                        name="name" 
                        required 
                        placeholder="Din virksomhed ApS"
                    >
                </div>

                <div class="form-group">
                    <label for="email">Email</label>
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        required 
                        placeholder="kontakt@dinvirksomhed.dk"
                    >
                </div>

                <div class="form-group">
                    <label for="website_url">Website</label>
                    <input 
                        type="url" 
                        id="website_url" 
                        name="website_url" 
                        required 
                        placeholder="https://www.dinvirksomhed.dk"
                    >
                    <span class="form-hint">Husk at inkludere https://</span>
                </div>

                <div class="form-group">
                    <label for="password">Adgangskode</label>
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        required 
                        minlength="6"
                        placeholder="Mindst 6 tegn"
                    >
                    <span class="form-hint">Mindst 6 tegn</span>
                </div>

                <button type="submit" class="submit-btn">
                    Opret konto
                </button>
            </form>

            <div class="auth-footer">
                Har du allerede en konto? 
                <a href="/affiliate/login">Log ind her</a>
            </div>
        </div>
    </div>
</body>
</html>

